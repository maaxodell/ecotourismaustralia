Dependencies:
The API that this website interacts with requires Node.js installed on your local machine:

Node.js JavaScript Runtime
https://nodejs.org/en/

Ensure 'npm package manager' is also selected to be installed in the wizard.

Once installed, double-click on 'launchSite'.